{
  "name": "rayleigh-stark-site",
  "version": "1.0.0",
  "private": true,
  "description": "Rayleigh Stark · Always-on monitoring for institutional books",
  "scripts": {
    "dev": "parcel index.html approach.html about.html products.html contact.html terms.html --port 1234",
    "build": "rm -rf dist .parcel-cache && parcel build index.html approach.html about.html products.html contact.html terms.html --dist-dir dist --public-url / && node scripts/postbuild.mjs",
    "preview": "python3 scripts/preview.py",
    "og": "node scripts/capture-og.mjs",
    "clean": "rm -rf dist .parcel-cache"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.16.4",
    "@parcel/transformer-jsonld": "^2.16.4",
    "parcel": "^2.14.0"
  },
  "overrides": {
    "browserslist": "4.24.4"
  }
}
